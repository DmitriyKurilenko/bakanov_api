services:
  web:
    build: .
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000 --timeout ${GUNICORN_TIMEOUT:-120} --graceful-timeout ${GUNICORN_GRACEFUL_TIMEOUT:-30} --workers ${GUNICORN_WORKERS:-2}
    env_file:
      - .env
    restart: unless-stopped
    # NO volume ./:/app — code must come from the built image only
    volumes:
      - static_data:/app/staticfiles
      - media_data:/app/media
    # No ports — traefik handles external access
    networks:
      - default
      - traefik-public
    labels:
<<<<<<< HEAD
      - 'traefik.enable=true'
      - 'traefik.http.routers.kapitan.rule=Host(`${DOMAIN}`)'
      - 'traefik.http.routers.kapitan.entrypoints=websecure'
      - 'traefik.http.routers.kapitan.tls.certresolver=letsencrypt'
      - 'traefik.http.services.kapitan.loadbalancer.server.port=8000'
=======
      - "traefik.enable=true"
      - "traefik.http.routers.kapitan.rule=Host(\`${DOMAIN}\`)"
      - "traefik.http.routers.kapitan.entrypoints=websecure"
      - "traefik.http.routers.kapitan.tls.certresolver=letsencrypt"
      - "traefik.http.services.kapitan.loadbalancer.server.port=8000"
>>>>>>> main
    depends_on:
      - db
      - redis

  db:
<<<<<<< HEAD
    image: postgres:16-alpine
=======
    image: postgres:18-alpine
>>>>>>> main
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql
    networks:
      - default

  redis:
<<<<<<< HEAD
    image: redis:7-alpine
=======
    image: redis:8-alpine
>>>>>>> main
    restart: unless-stopped
    networks:
      - default

  celery_worker:
    build: .
    command: celery -A config worker -l info
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - static_data:/app/staticfiles
      - media_data:/app/media
    networks:
      - default
    depends_on:
      - web
      - redis

  celery_beat:
    build: .
    command: celery -A config beat -l info
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - static_data:/app/staticfiles
      - media_data:/app/media
    networks:
      - default
    depends_on:
      - web
      - redis

  flower:
    build: .
    command: celery -A config flower --port=5555
    env_file:
      - .env
    restart: unless-stopped
    # Flower accessible only via localhost/SSH tunnel
    ports:
      - "127.0.0.1:5555:5555"
    networks:
      - default
    depends_on:
      - redis

volumes:
  postgres_data:
  static_data:
  media_data:

networks:
  default:
  traefik-public:
    external: true
